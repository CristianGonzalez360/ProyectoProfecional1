package presentacion;

class WorkbenchPresenterTest {

	/*
	 * TestView view = new TestView(); PaisController controller =
	 * Mockito.mock(PaisController.class); TestPresenter presenter = new
	 * TestPresenter(view, controller);
	 * 
	 * @Test void testColorInputChange() { JTextField input = (JTextField)
	 * ViewUtils.getChildNamed(view, TestView.INPUT); input.setText(""); JButton
	 * button = (JButton) ViewUtils.getChildNamed(view, TestView.BUTTON);
	 * button.doClick();
	 * Assertions.assertTrue(input.getBackground().equals(Color.YELLOW)); }
	 * 
	 * @Test void testColorInputDoesntChange() { JTextField input = (JTextField)
	 * ViewUtils.getChildNamed(view, TestView.INPUT); input.setText("Susan Doe");
	 * JButton button = (JButton) ViewUtils.getChildNamed(view, TestView.BUTTON);
	 * button.doClick();
	 * Assertions.assertTrue(input.getBackground().equals(Color.WHITE)); }
	 */
}